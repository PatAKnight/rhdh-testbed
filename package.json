{
  "name": "rhdh-testbed",
  "private": true,
  "scripts": {
    "shellcheck": "shellcheck --severity=warning --color=always **/*.sh",
    "prettier:check": "prettier --check '**/*.{yaml,yml,md}'",
    "prettier:fix": "prettier --write '**/*.{yaml,yml,md}'"
  },
  "devDependencies": {
    "prettier": "3.8.1",
    "prettier-plugin-sh": "0.18.0",
    "shellcheck": "4.1.0"
  }
}
