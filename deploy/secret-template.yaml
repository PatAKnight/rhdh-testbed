# WARNING: Do NOT add actual secrets to this file!
# This is a TEMPLATE only. Create your own secret:
#
#   cp deploy/secret.yaml deploy/secret.local.yaml
#   # Edit secret.local.yaml with your values
#   oc apply -f deploy/secret.local.yaml
#
# Files matching *.local.yaml are gitignored.
apiVersion: v1
kind: Secret
metadata:
  name: rhdh-testbed-secrets
  namespace: rhdh-testbed
  labels:
    app.kubernetes.io/name: rhdh-testbed
    app.kubernetes.io/component: config
type: Opaque
stringData:
  # Only needed if configuring a DIFFERENT cluster than where Job runs
  # Leave empty to use in-cluster ServiceAccount authentication
  K8S_CLUSTER_TOKEN: ''
  K8S_CLUSTER_URL: ''

  # Optional: GitHub integration
  GITHUB_TOKEN: ''
  GITHUB_APP_ID: ''
  GITHUB_APP_CLIENT_ID: ''
  GITHUB_APP_CLIENT_SECRET: ''

  # Optional: GitLab integration
  GITLAB_TOKEN: ''

  # Optional: OCM settings
  OCM_HUB_NAME: ''
  OCM_HUB_OWNERS: ''
